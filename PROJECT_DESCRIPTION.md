# 📋 Полное описание проекта Klyro

## 🎯 Общее описание

**Klyro** — это Telegram Web App приложение для расчёта калорий и управления питанием. Проект состоит из двух основных компонентов:

1. **Backend (Python/Flask)** — сервер для обработки команд Telegram бота
2. **Frontend (HTML/CSS/JavaScript)** — веб-приложение, работающее внутри Telegram

---

## 🏗️ Архитектура проекта

### Компоненты системы

```
┌─────────────────┐
│   Telegram API   │
└────────┬─────────┘
         │
         │ Polling/Webhook
         │
┌────────▼─────────┐
│  Backend Server  │  ← Flask + Gunicorn на Railway
│  (bot_server.py) │
└────────┬─────────┘
         │
         │ HTTP запросы
         │
┌────────▼─────────┐
│  Frontend Web App │  ← HTML/CSS/JS на GitHub Pages
│  (index.html)     │
└───────────────────┘
```

---

## 🔧 Backend (bot_server.py)

### Технологии
- **Flask 3.0.0** — веб-фреймворк
- **Gunicorn 21.2.0** — WSGI сервер для production
- **Requests 2.31.0** — HTTP клиент для Telegram API
- **Threading** — для фоновых задач

### Основные функции

#### 1. **Polling механизм** (основной способ получения обновлений)
```python
def polling_loop():
    # Периодически запрашивает обновления у Telegram
    # Интервал: 30 секунд (timeout)
    # Обрабатывает команду /start
```

**Как работает:**
- Бот каждые 30 секунд отправляет запрос `getUpdates` к Telegram API
- Получает все новые сообщения с момента последнего обновления
- Обрабатывает команду `/start` и отправляет приветственное сообщение с кнопкой "ОТКРЫТЬ"

**Почему polling, а не webhook:**
- Railway на бесплатном плане может останавливать контейнеры
- Webhook не работает, если сервер недоступен
- Polling работает — при запуске контейнера бот сразу получает все пропущенные сообщения

#### 2. **Keep-Alive механизм**
```python
def keep_alive():
    # Периодически пингует /health endpoint
    # Предотвращает остановку контейнера Railway
```

**Как работает:**
- Фоновый поток каждые 30 секунд отправляет GET запрос к `/health`
- Поддерживает активность контейнера на Railway
- Предотвращает автоматическое отключение из-за неактивности

#### 3. **Обработка команды /start**
```python
def process_update(update):
    if text == '/start':
        # Отправляет приветственное сообщение
        # С кнопкой "ОТКРЫТЬ" (inline keyboard с web_app)
```

**Что происходит:**
1. Пользователь отправляет `/start` боту
2. Polling получает обновление
3. Бот отправляет приветственное сообщение с кнопкой
4. Кнопка содержит URL Web App (`WEB_APP_URL`)

#### 4. **Endpoints**

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Проверка работы сервера |
| `/health` | GET | Health check для Railway |
| `/ping` | GET | Простой ping для keep-alive |
| `/webhook` | POST/GET | Обработка webhook (резервный) |
| `/set_webhook` | GET | Установка webhook URL |
| `/log` | POST | Приём логов от клиента |

### Переменные окружения

```bash
BOT_TOKEN=8515314140:...        # Токен бота от BotFather
WEB_APP_URL=https://...         # URL веб-приложения (GitHub Pages)
PORT=8080                       # Порт сервера (автоматически на Railway)
```

### Развёртывание на Railway

1. **Procfile:**
   ```
   web: gunicorn bot_server:app --bind 0.0.0.0:$PORT
   ```

2. **railway.json:**
   - Использует Nixpacks для сборки
   - Автоматически определяет Python окружение
   - Устанавливает зависимости из `requirements.txt`

3. **Запуск:**
   - Railway автоматически запускает Gunicorn
   - Polling и keep-alive запускаются в фоновых потоках
   - Сервер слушает на порту из переменной `PORT`

---

## 🎨 Frontend (Web App)

### Технологии
- **HTML5** — структура
- **CSS3** — стили с тёмной темой (dark blue)
- **Vanilla JavaScript** — логика без фреймворков
- **Telegram Web App API** — интеграция с Telegram
- **LocalStorage** — хранение данных пользователя

### Структура файлов

#### 1. **index.html**
- Основная HTML-страница
- Содержит все экраны приложения
- Подключает Telegram Web App API скрипт
- Инициализирует приложение через `app.js`

**Экраны:**
- `loading-screen` — экран загрузки с логотипом
- `auth-screen` — экран авторизации
- `onboarding-screen` — онбординг (4 шага)
- `profile-screen` — профиль пользователя

#### 2. **app.js** — логика приложения

**Основные функции:**

##### Инициализация
```javascript
initApp() → checkUserAuth() → showScreen()
```

**Поток работы:**
1. Проверяет наличие Telegram Web App API
2. Инициализирует `tg.ready()` и `tg.expand()`
3. Проверяет сохранённые данные в LocalStorage
4. Проверяет данные Telegram (`initDataUnsafe.user`)
5. Показывает соответствующий экран

##### Авторизация
```javascript
checkUserAuth()
```
- Проверяет LocalStorage на наличие данных
- Проверяет Telegram `initDataUnsafe.user`
- Если есть данные → показывает профиль
- Если нет данных → показывает онбординг
- Если нет Telegram → показывает экран авторизации

##### Онбординг (4 шага)
1. **Шаг 1:** Возраст и пол
2. **Шаг 2:** Рост и вес
3. **Шаг 3:** Уровень активности (низкий/средний/высокий)
4. **Шаг 4:** Цель (снижение/поддержание/набор веса)

**Валидация:**
- Каждый шаг валидируется перед переходом к следующему
- Показываются уведомления об ошибках через `tg.showAlert()`

##### Расчёт калорий
```javascript
calculateCalories()
```

**Формула Mifflin-St Jeor:**
- **BMR (мужчины):** `10 × вес + 6.25 × рост - 5 × возраст + 5`
- **BMR (женщины):** `10 × вес + 6.25 × рост - 5 × возраст - 161`
- **TDEE:** `BMR × коэффициент_активности`
- **Финальные калории:** `TDEE × корректировка_цели`

**Коэффициенты:**
- Низкая активность: 1.2
- Средняя активность: 1.55
- Высокая активность: 1.9

**Корректировка цели:**
- Снижение веса: ×0.85 (-15%)
- Поддержание: ×1.0 (без изменений)
- Набор массы: ×1.15 (+15%)

##### Сохранение данных
```javascript
saveUserData()
```
- Сохраняет все данные пользователя в `localStorage` под ключом `klyro_user_data`
- Формат: JSON строка
- Данные сохраняются после завершения онбординга и при редактировании профиля

#### 3. **styles.css** — стили

**Тёмная тема (Dark Blue):**
```css
--background-color: #1a2332;      /* Тёмно-синий фон */
--background-card: #243447;       /* Карточки */
--text-primary: #E8F4F8;           /* Светлый текст */
--text-secondary: #B0C4DE;         /* Вторичный текст */
--primary-color: #5DADE2;         /* Основной цвет (синий) */
--secondary-color: #82E0AA;        /* Вторичный цвет (зелёный) */
```

**Особенности:**
- Все экраны скрыты по умолчанию (`display: none`, `visibility: hidden`, `opacity: 0`)
- Активный экран: `display: block`, `visibility: visible`, `opacity: 1`
- Плавные переходы между экранами (fadeIn анимация)
- Адаптивный дизайн для мобильных устройств

### Развёртывание на GitHub Pages

1. Репозиторий: `kobravania/klyro_app`
2. URL: `https://kobravania.github.io/klyro_app/`
3. Файлы: `index.html`, `app.js`, `styles.css` в корне репозитория
4. Автоматический деплой при push в `main` ветку

---

## 🔄 Поток данных

### Сценарий 1: Пользователь запускает бота

```
1. Пользователь → Telegram → /start
2. Telegram → Polling → bot_server.py
3. bot_server.py → Telegram API → sendMessage()
4. Telegram → Пользователь → Приветственное сообщение + кнопка "ОТКРЫТЬ"
5. Пользователь → Нажимает "ОТКРЫТЬ"
6. Telegram → Открывает Web App (index.html)
7. Web App → Проверяет LocalStorage → Проверяет Telegram initData
8. Web App → Показывает экран (auth/onboarding/profile)
```

### Сценарий 2: Пользователь заполняет онбординг

```
1. Пользователь → Вводит данные (возраст, пол, рост, вес, активность, цель)
2. app.js → Валидация каждого шага
3. app.js → calculateCalories() → Расчёт калорий
4. app.js → saveUserData() → Сохранение в LocalStorage
5. app.js → showProfileScreen() → Отображение профиля
```

### Сценарий 3: Пользователь открывает приложение повторно

```
1. Telegram → Открывает Web App
2. app.js → checkUserAuth()
3. app.js → localStorage.getItem('klyro_user_data')
4. app.js → JSON.parse() → Восстановление данных
5. app.js → showProfileScreen() → Показ профиля с сохранёнными данными
```

---

## 🔐 Безопасность

### Токен бота
- **НЕ хранится в коде** — только в переменных окружения Railway
- **НЕ коммитится в Git** — добавлен в `.gitignore`
- При компрометации токена — немедленная замена через BotFather

### Данные пользователя
- Хранятся только в LocalStorage браузера
- Не отправляются на сервер (кроме логов ошибок)
- Telegram авторизация через `initDataUnsafe` (проверяется Telegram)

---

## 🚀 Процесс развёртывания

### Backend (Railway)

1. **Подготовка:**
   ```bash
   # Файлы уже готовы:
   - bot_server.py
   - requirements.txt
   - Procfile
   - railway.json
   ```

2. **Настройка Railway:**
   - Создать проект на Railway.app
   - Подключить GitHub репозиторий
   - Установить переменные окружения:
     - `BOT_TOKEN=8515314140:...`
     - `WEB_APP_URL=https://kobravania.github.io/klyro_app/`
   - Railway автоматически деплоит при push

3. **Проверка:**
   - Логи Railway показывают `[POLLING] Starting polling loop...`
   - Отправка `/start` боту → получение приветственного сообщения

### Frontend (GitHub Pages)

1. **Подготовка:**
   ```bash
   # Файлы в корне репозитория:
   - index.html
   - app.js
   - styles.css
   ```

2. **Настройка GitHub Pages:**
   - Settings → Pages → Source: `main` branch
   - URL: `https://username.github.io/klyro_app/`

3. **Проверка:**
   - Открыть URL в браузере → должно открыться приложение
   - Открыть через кнопку в боте → должно работать в Telegram

---

## 📊 Особенности реализации

### 1. Предотвращение мелькания экранов
- Все экраны скрыты по умолчанию через CSS (`display: none`, `visibility: hidden`, `opacity: 0`)
- Проверка данных происходит синхронно перед показом экрана
- Удалены все `setTimeout` задержки, которые могли вызывать мелькание

### 2. Keep-Alive для Railway
- Фоновый поток пингует `/health` каждые 30 секунд
- Предотвращает остановку контейнера из-за неактивности
- Критично для бесплатного плана Railway

### 3. Polling вместо Webhook
- Надёжнее для нестабильного хостинга
- Работает даже при периодических остановках контейнера
- Получает все пропущенные сообщения при запуске

### 4. Обработка ошибок
- Все функции обёрнуты в `try-catch`
- Логирование ошибок в консоль и на сервер (через `/log`)
- Graceful fallback при отсутствии Telegram API

### 5. Валидация данных
- Проверка всех полей перед переходом к следующему шагу
- Показ понятных сообщений об ошибках
- Предотвращение некорректных данных

---

## 🛠️ Технологический стек

### Backend
- **Python 3.x**
- **Flask 3.0.0** — веб-фреймворк
- **Gunicorn 21.2.0** — WSGI сервер
- **Requests 2.31.0** — HTTP клиент
- **Threading** — многопоточность

### Frontend
- **HTML5** — разметка
- **CSS3** — стили (custom properties, animations)
- **Vanilla JavaScript** — логика
- **Telegram Web App API** — интеграция
- **LocalStorage API** — хранение данных

### Инфраструктура
- **Railway.app** — хостинг backend
- **GitHub Pages** — хостинг frontend
- **Telegram Bot API** — API бота
- **Nixpacks** — система сборки (Railway)

---

## 📁 Структура проекта

```
klyro_app/
├── bot_server.py              # Backend сервер (Flask)
├── requirements.txt           # Python зависимости
├── Procfile                   # Команда запуска для Railway
├── railway.json               # Конфигурация Railway
│
├── index.html                 # Главная страница Web App
├── app.js                     # Логика приложения
├── styles.css                 # Стили (тёмная тема)
│
├── README.md                  # Основная документация
├── PROJECT_DESCRIPTION.md     # Этот файл
├── DEPLOYMENT.md              # Инструкции по развёртыванию
├── RAILWAY_DEPLOY.md          # Настройка Railway
├── SETUP_GITHUB.md            # Настройка GitHub Pages
│
└── [другие вспомогательные файлы]
```

---

## 🎯 Текущий статус

✅ **Работает:**
- Polling механизм получения обновлений
- Обработка команды `/start`
- Отправка приветственного сообщения с кнопкой
- Web App открывается в Telegram
- Онбординг (4 шага)
- Расчёт калорий по формуле Mifflin-St Jeor
- Сохранение данных в LocalStorage
- Отображение профиля
- Редактирование профиля
- Тёмная тема (dark blue)
- Keep-alive для Railway

✅ **Развёрнуто:**
- Backend на Railway.app
- Frontend на GitHub Pages
- Бот работает стабильно

---

## 🔮 Возможные улучшения

1. **База данных:**
   - Заменить LocalStorage на серверную БД (PostgreSQL)
   - Синхронизация данных между устройствами

2. **Дополнительные функции:**
   - Трекер приёмов пищи
   - Подсчёт макронутриентов (белки, жиры, углеводы)
   - История веса и графики прогресса
   - Напоминания о приёме пищи
   - Планы питания

3. **Улучшения UX:**
   - Анимации переходов между экранами
   - Офлайн режим
   - Экспорт данных

4. **Безопасность:**
   - Верификация `initData` от Telegram
   - Шифрование данных в LocalStorage
   - HTTPS для всех запросов

---

## 📝 Заключение

Проект Klyro представляет собой полнофункциональное Telegram Web App приложение для расчёта калорий. Архитектура разделена на backend (Flask на Railway) и frontend (HTML/CSS/JS на GitHub Pages). Использование polling механизма обеспечивает надёжную работу даже на нестабильном хостинге. Приложение готово к использованию и может быть расширено дополнительными функциями.

---

**Дата создания документа:** 2025-11-30  
**Версия проекта:** 1.0  
**Статус:** ✅ Работает и развёрнуто





