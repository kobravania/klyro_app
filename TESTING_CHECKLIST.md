# ✅ Чеклист для тестирования изменений

## Проблема: "Изменения не применились"

### 🔍 Проверка 1: Файлы на месте
```bash
# Проверьте наличие файлов:
ls -la index.html app.js styles.css data/products.json
```

### 🔄 Проверка 2: Очистка кэша браузера

**В Chrome/Edge:**
1. Откройте DevTools (F12)
2. Правый клик на кнопке обновления
3. Выберите "Очистить кэш и жёсткая перезагрузка"

**Или:**
- Ctrl+Shift+R (Windows/Linux)
- Cmd+Shift+R (Mac)

### 📤 Проверка 3: Загрузка на GitHub Pages

Если тестируете через GitHub Pages:

```bash
# 1. Проверьте статус git
git status

# 2. Добавьте все изменения
git add .

# 3. Закоммитьте
git commit -m "Добавлены новые модули: трекер питания, дневник, история, тренировки"

# 4. Запушьте
git push origin main
```

**Важно:** После push подождите 1-2 минуты, пока GitHub Pages обновится.

### 🧪 Проверка 4: Локальное тестирование

Если тестируете локально:

```bash
# Запустите локальный сервер (важно для загрузки products.json)
python3 -m http.server 8000

# Или
npx http-server -p 8000
```

Затем откройте: `http://localhost:8000`

### ✅ Что должно появиться:

1. **В профиле (Dashboard):**
   - Блок "Сегодня" с калориями
   - График макронутриентов (donut chart)
   - Кнопка "Добавить приём пищи"
   - Кнопки: Дневник, История, Тренировки, Настройки

2. **Новые экраны:**
   - Добавление еды (`/add-food`)
   - Дневник питания (`/diary`)
   - История (`/history`)
   - Тренировки (`/activity`)
   - Настройки (`/settings`)

### 🐛 Если всё ещё не работает:

1. **Проверьте консоль браузера (F12):**
   - Есть ли ошибки JavaScript?
   - Загружается ли `data/products.json`?

2. **Проверьте Network tab:**
   - Загружается ли `styles.css?v=5`?
   - Загружается ли `app.js`?

3. **Проверьте версию CSS:**
   - В `index.html` должно быть: `styles.css?v=5`

### 📝 Быстрая проверка изменений:

```bash
# Проверьте наличие новых элементов:
grep -c "today-stats-card" index.html  # Должно быть: 1
grep -c "add-food-screen" index.html    # Должно быть: 1
grep -c "showAddFoodScreen" app.js      # Должно быть: 2
grep -c "today-stats-card" styles.css   # Должно быть: 1
```

Если все проверки пройдены, но изменения не видны — проблема в кэше браузера или файлы не загружены на сервер.




