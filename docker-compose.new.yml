version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: klyro
      POSTGRES_USER: klyro
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-klyro_password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U klyro"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend_new
      dockerfile: Dockerfile
    environment:
      POSTGRES_DB: klyro
      POSTGRES_USER: klyro
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-klyro_password}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      BOT_TOKEN: ${BOT_TOKEN}
      SECRET_KEY: ${SECRET_KEY:-django-insecure-change-me}
      DEBUG: ${DEBUG:-False}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend_new:/app
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"

  frontend:
    build:
      context: ./frontend_new
      dockerfile: Dockerfile
    volumes:
      - ./frontend_new:/app
      - /app/node_modules
    ports:
      - "3000:3000"

volumes:
  postgres_data:

