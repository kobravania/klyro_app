# 🚀 Деплой Klyro на Bitlaunch (Ubuntu)

Полное руководство по развертыванию приложения Klyro на сервере Bitlaunch с использованием Docker.

## 📋 Требования

- Сервер Bitlaunch с Ubuntu 20.04+ или 22.04
- Доменное имя, указывающее на IP сервера (например, `klyro.app`)
- SSH доступ к серверу
- Минимум 2GB RAM, 20GB диска

## 🏗️ Архитектура

Проект состоит из следующих сервисов:

- **PostgreSQL** - база данных (порт 5432, только локально)
- **Backend** - Flask API сервер (порт 5000, внутренний)
- **Bot** - Telegram бот (внутренний)
- **Frontend** - Nginx для статических файлов (порты 80, 443)
- **Certbot** - автоматическое обновление SSL сертификатов

## 📦 Шаг 1: Подготовка сервера

### 1.1 Подключение к серверу

```bash
ssh root@your-server-ip
```

### 1.2 Обновление системы

```bash
apt-get update && apt-get upgrade -y
```

## 🚀 Шаг 2: Установка проекта

### 2.1 Клонирование репозитория

```bash
cd /opt
git clone https://github.com/your-username/klyro_app.git klyro
cd klyro
```

### 2.2 Запуск скрипта установки

```bash
chmod +x deploy/setup.sh
sudo ./deploy/setup.sh
```

Скрипт автоматически:
- Установит Docker и Docker Compose
- Настроит firewall
- Склонирует проект (если еще не склонирован)
- Создаст `.env` файлы из шаблонов
- Получит SSL сертификаты
- Запустит все сервисы

### 2.3 Настройка переменных окружения

Перед первым запуском отредактируйте `.env` файл:

```bash
nano .env
```

Обязательные переменные:

```env
DOMAIN=klyro.app
SSL_EMAIL=your-email@example.com
POSTGRES_PASSWORD=strong_password_here
BOT_TOKEN=your_telegram_bot_token
```

После редактирования сохраните файл (Ctrl+O, Enter, Ctrl+X).

## 🔧 Шаг 3: Ручная настройка (если нужно)

### 3.1 Создание .env файла вручную

Если скрипт не создал `.env`, скопируйте шаблон:

```bash
cp .env.example .env
nano .env
```

### 3.2 Настройка DNS

Убедитесь, что домен указывает на IP вашего сервера:

```bash
# Проверка DNS
dig klyro.app
nslookup klyro.app
```

Должен вернуться IP вашего сервера.

### 3.3 Получение SSL сертификата

Если автоматическое получение не сработало:

```bash
docker-compose run --rm certbot certonly \
    --webroot \
    --webroot-path=/var/www/certbot \
    --email your-email@example.com \
    --agree-tos \
    --no-eff-email \
    -d klyro.app \
    -d www.klyro.app
```

### 3.4 Запуск сервисов

```bash
docker-compose up -d
```

## 🔍 Шаг 4: Проверка работы

### 4.1 Проверка статуса контейнеров

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up`.

### 4.2 Проверка логов

```bash
# Все логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f bot
```

### 4.3 Проверка health checks

```bash
# Backend health check
curl http://localhost:5000/health

# Через Nginx
curl https://klyro.app/health
```

### 4.4 Проверка в браузере

Откройте в браузере: `https://klyro.app`

## 🔄 Шаг 5: Обновление проекта

Для обновления проекта используйте скрипт:

```bash
cd /opt/klyro
./deploy/update.sh
```

Скрипт автоматически:
- Создаст бэкап базы данных
- Получит обновления из Git
- Пересоберет измененные сервисы
- Применит миграции (если есть)
- Обновит SSL сертификаты
- Перезапустит сервисы

## 🛠️ Ручное управление

### Запуск/остановка

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Перезапуск конкретного сервиса
docker-compose restart backend
docker-compose restart frontend
```

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f bot
docker-compose logs -f postgres
```

### Подключение к базе данных

```bash
# Через docker-compose
docker-compose exec postgres psql -U klyro -d klyro

# Или напрямую
docker exec -it klyro_postgres psql -U klyro -d klyro
```

### Бэкап базы данных

```bash
# Создание бэкапа
docker-compose exec -T postgres pg_dump -U klyro klyro > backup_$(date +%Y%m%d).sql

# Восстановление из бэкапа
docker-compose exec -T postgres psql -U klyro klyro < backup_20231206.sql
```

## 🔒 Безопасность

### Firewall

Firewall настроен автоматически, открыты порты:
- 22 (SSH)
- 80 (HTTP)
- 443 (HTTPS)

### SSL сертификаты

Сертификаты автоматически обновляются через cron job (настроен в `setup.sh`).

Проверка автообновления:

```bash
crontab -l
```

### Обновление паролей

Для изменения пароля PostgreSQL:

1. Отредактируйте `.env` файл
2. Обновите пароль в базе:

```bash
docker-compose exec postgres psql -U klyro -d klyro -c "ALTER USER klyro WITH PASSWORD 'new_password';"
```

3. Перезапустите сервисы:

```bash
docker-compose restart
```

## 🐛 Решение проблем

### Проблема: Контейнеры не запускаются

```bash
# Проверка логов
docker-compose logs

# Проверка конфигурации
docker-compose config

# Пересборка образов
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: SSL сертификат не получен

1. Проверьте, что домен указывает на IP сервера
2. Проверьте, что порт 80 открыт
3. Попробуйте получить сертификат вручную (см. Шаг 3.3)

### Проблема: База данных не подключается

```bash
# Проверка статуса PostgreSQL
docker-compose exec postgres pg_isready -U klyro

# Проверка подключения
docker-compose exec backend python -c "import psycopg2; psycopg2.connect('postgresql://klyro:password@postgres:5432/klyro')"
```

### Проблема: Nginx не запускается

```bash
# Проверка конфигурации Nginx
docker-compose exec frontend nginx -t

# Просмотр логов
docker-compose logs frontend
```

## 📊 Мониторинг

### Использование ресурсов

```bash
# Использование диска
df -h

# Использование памяти
free -h

# Использование Docker
docker system df
```

### Очистка неиспользуемых ресурсов

```bash
# Очистка неиспользуемых образов
docker image prune -f

# Очистка неиспользуемых контейнеров
docker container prune -f

# Полная очистка (осторожно!)
docker system prune -a -f
```

## 📝 Структура проекта

```
klyro_app/
├── backend/
│   ├── Dockerfile
│   └── .env.example
├── bot/
│   ├── Dockerfile
│   └── .env.example
├── frontend/
│   └── Dockerfile
├── nginx/
│   ├── nginx.conf
│   └── conf.d/
│       └── default.conf
├── deploy/
│   ├── setup.sh
│   └── update.sh
├── docker-compose.yml
├── .env.example
└── BITLAUNCH_DEPLOY.md
```

## 🔗 Полезные ссылки

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Nginx Documentation](https://nginx.org/en/docs/)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)

## ⚠️ Важные замечания

1. **Не изменяйте структуру базы данных** - все миграции должны быть обратно совместимы
2. **Не удаляйте volume `postgres_data`** - там хранятся данные базы
3. **Регулярно создавайте бэкапы** - используйте скрипт обновления или создавайте вручную
4. **Храните `.env` файлы в безопасности** - не коммитьте их в Git
5. **Мониторьте логи** - регулярно проверяйте логи на ошибки

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Проверьте конфигурацию: `docker-compose config`
4. Создайте issue в репозитории с описанием проблемы

---

**Успешного деплоя! 🚀**


