# Деплой Klyro Bot на Railway

## Шаг 1: Регистрация на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub (используйте ваш аккаунт `kobravania`)

## Шаг 2: Создание проекта

### Вариант A: Через веб-интерфейс (проще)

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите репозиторий `kobravania/klyro_app`
4. Railway автоматически определит Python и начнёт деплой

### Вариант B: Через CLI

```bash
# Установите Railway CLI (если ещё не установлен)
curl -fsSL https://railway.app/install.sh | sh

# Войдите
railway login

# Инициализируйте проект
cd /Users/kobra/PycharmProjects/klyro_app
railway init

# Задеплойте
railway up
```

## Шаг 3: Настройка переменных окружения

В веб-интерфейсе Railway:

1. Откройте ваш проект
2. Перейдите в **Settings** → **Variables**
3. Добавьте переменные:
   - `BOT_TOKEN` = `YOUR_BOT_TOKEN_HERE`
   - `WEB_APP_URL` = `https://kobravania.github.io/klyro_app/`
   - `PORT` = `5000` (Railway установит автоматически)

Или через CLI:
```bash
railway variables set BOT_TOKEN=YOUR_BOT_TOKEN_HERE
railway variables set WEB_APP_URL=https://kobravania.github.io/klyro_app/
```

## Шаг 4: Получение URL

После деплоя Railway даст вам URL вида:
`https://klyro-bot-production.up.railway.app`

Или получите через CLI:
```bash
railway domain
```

## Шаг 5: Установка Webhook

Замените `ВАШ_RAILWAY_URL` на ваш URL:

```bash
curl "https://api.telegram.org/botYOUR_BOT_TOKEN_HERE/setWebhook?url=ВАШ_RAILWAY_URL/webhook&drop_pending_updates=true"
```

Например:
```bash
curl "https://api.telegram.org/botYOUR_BOT_TOKEN_HERE/setWebhook?url=https://klyro-bot-production.up.railway.app/webhook&drop_pending_updates=true"
```

## Шаг 6: Проверка

1. Проверьте webhook:
```bash
curl "https://api.telegram.org/botYOUR_BOT_TOKEN_HERE/getWebhookInfo"
```

2. Отправьте `/start` боту - он должен автоматически ответить!

## Преимущества Railway

✅ Бесплатный план (500 часов/месяц)  
✅ Постоянный URL (не меняется)  
✅ Автоматический деплой из GitHub  
✅ Простое управление переменными  
✅ Логи в реальном времени  

## Мониторинг

В Railway вы можете:
- Видеть логи в реальном времени
- Мониторить использование ресурсов
- Настраивать автодеплой при push в GitHub

## Готово!

После настройки команда `/start` будет работать автоматически, и кнопка "ОТКРЫТЬ" появится в списке чатов! 🎉

