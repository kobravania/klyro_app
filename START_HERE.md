# 🚀 НАЧНИТЕ ОТСЮДА - Деплой на Bitlaunch

## ⚡ Быстрый старт (3 команды)

### 1. Подключитесь к серверу Bitlaunch

```bash
ssh root@YOUR_SERVER_IP
```

**Где взять IP?** 
- В панели Bitlaunch найдите ваш сервер
- IP адрес будет указан там

### 2. Склонируйте и запустите

```bash
cd /opt && git clone https://github.com/kobravania/klyro_app.git klyro && cd klyro && chmod +x deploy/setup.sh && sudo ./deploy/setup.sh
```

### 3. Настройте .env когда скрипт попросит

Отредактируйте файл `.env`:
- `DOMAIN=ваш-домен.com`
- `SSL_EMAIL=ваш-email@example.com`  
- `POSTGRES_PASSWORD=придумайте_пароль`
- `BOT_TOKEN=ваш_telegram_bot_token` (если есть)

**Готово!** Приложение будет доступно по адресу: `https://ваш-домен.com`

---

## 📋 Что нужно подготовить ДО деплоя:

1. ✅ **Доменное имя** - должно указывать на IP вашего сервера
   - Проверка: `ping ваш-домен.com` должен вернуть IP сервера
   
2. ✅ **Email** - для получения SSL сертификата (Let's Encrypt)

3. ✅ **Telegram Bot Token** (опционально, если используете бота)
   - Получить у [@BotFather](https://t.me/BotFather)

---

## 🔍 После деплоя - проверка:

```bash
# Статус всех сервисов
docker-compose ps

# Должны быть все в статусе "Up"

# Проверка логов
docker-compose logs -f

# Проверка в браузере
# Откройте: https://ваш-домен.com
```

---

## ❓ Частые вопросы:

**Q: Нужен ли токен Bitlaunch?**  
A: Нет! Нужен только SSH доступ к серверу.

**Q: Что если домен еще не настроен?**  
A: Можно запустить без SSL сначала, потом добавить сертификат вручную.

**Q: Как обновить проект?**  
A: `cd /opt/klyro && ./deploy/update.sh`

**Q: Где логи?**  
A: `docker-compose logs -f` или `docker-compose logs -f backend`

---

## 📚 Подробная документация:

- **QUICK_DEPLOY.md** - быстрая инструкция
- **BITLAUNCH_DEPLOY.md** - полное руководство со всеми деталями

---

**Готовы? Подключайтесь к серверу и запускайте! 🚀**

