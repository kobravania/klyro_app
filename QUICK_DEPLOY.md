# ⚡ Быстрый деплой на Bitlaunch

## Что нужно от вас:

1. **IP адрес сервера Bitlaunch** (например: `123.45.67.89`)
2. **SSH доступ** (обычно `root` или пользователь с sudo)
3. **Доменное имя** (например: `klyro.app`) - должно указывать на IP сервера
4. **Email для SSL** (для Let's Encrypt)
5. **Telegram Bot Token** (если используете бота)

## 🚀 Пошаговая инструкция:

### Шаг 1: Подключитесь к серверу

```bash
ssh root@YOUR_SERVER_IP
```

### Шаг 2: Склонируйте проект

```bash
cd /opt
git clone https://github.com/kobravania/klyro_app.git klyro
cd klyro
```

### Шаг 3: Запустите установку

```bash
chmod +x deploy/setup.sh
sudo ./deploy/setup.sh
```

Скрипт автоматически:
- ✅ Установит Docker и Docker Compose
- ✅ Настроит firewall
- ✅ Создаст `.env` файл из шаблона
- ✅ Попросит вас отредактировать `.env`
- ✅ Получит SSL сертификат
- ✅ Запустит все сервисы

### Шаг 4: Настройте .env файл

Когда скрипт попросит, отредактируйте `.env`:

```bash
nano .env
```

Обязательно измените:
- `DOMAIN=ваш-домен.com`
- `SSL_EMAIL=ваш-email@example.com`
- `POSTGRES_PASSWORD=сильный_пароль`
- `BOT_TOKEN=ваш_telegram_bot_token` (если используете)

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 5: Продолжите установку

Нажмите `Enter` в терминале, скрипт продолжит работу.

### Шаг 6: Проверьте работу

Откройте в браузере: `https://ваш-домен.com`

## 🔍 Проверка статуса

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
```

## ⚠️ Если что-то пошло не так:

1. **SSL сертификат не получен:**
   - Убедитесь, что домен указывает на IP сервера
   - Проверьте, что порт 80 открыт
   - Получите сертификат вручную (см. BITLAUNCH_DEPLOY.md)

2. **Контейнеры не запускаются:**
   ```bash
   docker-compose logs
   docker-compose down
   docker-compose up -d
   ```

3. **Проблемы с базой данных:**
   ```bash
   docker-compose exec postgres pg_isready -U klyro
   ```

## 📝 После успешного деплоя:

Для обновления проекта:
```bash
cd /opt/klyro
./deploy/update.sh
```

---

**Готово! Ваше приложение работает на Bitlaunch! 🎉**

